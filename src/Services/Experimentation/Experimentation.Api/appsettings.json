{
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "ConnectionString": "mongodb://localhost:27017",
  "Database": "ExperimentsDB",

  "Swagger": {
    "FileName": "Experimentation.Api.xml"
    //"BaseUrl": "ExperimentationApi"
  }
}

// I added the BaseUrl as a means of determing the relative url where the swagger document json file is.
// Turns out i did not need it.