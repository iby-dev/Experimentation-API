{
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "ConnectionString": "mongodb://localhost:27017",
  "Database": "ExperimentsDB",

  "Swagger": {
    "FileName": "Experimentation.Api.xml"
    //"BaseUrl": "ExperimentationApi"
  }
}

// Use this file to add Dev specific settings relevant to a Dev environment.
// In order for this config file to be selected during publishing then the following environment variable needs to be present on the server:
// ASPNETCORE_ENVIRONMENT
//i.e on a dev server I am using currently, I have shortend development down to 'Dev'. Windows and MacOs are case insensitive in this regard.